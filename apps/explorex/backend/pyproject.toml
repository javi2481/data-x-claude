[project]
name = "explorex-backend"
version = "0.1.0"
description = "Explorex app backend"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.135.1",
    "python-dotenv>=1.2.2",
    "python-multipart>=0.0.20",
    "uvicorn>=0.41.0",
]

[tool.uv]
package = false
